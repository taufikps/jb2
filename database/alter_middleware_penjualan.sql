-- Schema update for middleware_penjualan to support richer Jubelio penjualan payload
ALTER TABLE middleware_penjualan
  ADD COLUMN action VARCHAR(100) DEFAULT NULL AFTER total_amount,
  ADD COLUMN order_status VARCHAR(100) DEFAULT NULL AFTER action,
  ADD COLUMN salesorder_id INT DEFAULT NULL AFTER order_status,
  ADD COLUMN salesorder_no VARCHAR(100) DEFAULT NULL AFTER salesorder_id,
  ADD COLUMN invoice_id INT DEFAULT NULL AFTER salesorder_no,
  ADD COLUMN invoice_no VARCHAR(100) DEFAULT NULL AFTER invoice_id,
  ADD COLUMN invoice_date DATETIME DEFAULT NULL AFTER invoice_no,
  ADD COLUMN contact_id INT DEFAULT NULL AFTER invoice_date,
  ADD COLUMN customer_name VARCHAR(255) DEFAULT NULL AFTER contact_id,
  ADD COLUMN customer_phone VARCHAR(50) DEFAULT NULL AFTER customer_name,
  ADD COLUMN customer_email VARCHAR(255) DEFAULT NULL AFTER customer_phone,
  ADD COLUMN transaction_date DATETIME DEFAULT NULL AFTER customer_email,
  ADD COLUMN created_date DATETIME DEFAULT NULL AFTER transaction_date,
  ADD COLUMN last_modified DATETIME DEFAULT NULL AFTER created_date,
  ADD COLUMN internal_status VARCHAR(100) DEFAULT NULL AFTER last_modified,
  ADD COLUMN channel_status VARCHAR(100) DEFAULT NULL AFTER internal_status,
  ADD COLUMN source VARCHAR(100) DEFAULT NULL AFTER channel_status,
  ADD COLUMN source_name VARCHAR(100) DEFAULT NULL AFTER source,
  ADD COLUMN store VARCHAR(100) DEFAULT NULL AFTER source_name,
  ADD COLUMN store_name VARCHAR(150) DEFAULT NULL AFTER store,
  ADD COLUMN store_id INT DEFAULT NULL AFTER store_name,
  ADD COLUMN location_id INT DEFAULT NULL AFTER store_id,
  ADD COLUMN location_name VARCHAR(150) DEFAULT NULL AFTER location_id,
  ADD COLUMN location_code VARCHAR(100) DEFAULT NULL AFTER location_name,
  ADD COLUMN sub_total DECIMAL(18,2) DEFAULT NULL AFTER location_code,
  ADD COLUMN total_disc DECIMAL(18,2) DEFAULT NULL AFTER sub_total,
  ADD COLUMN total_tax DECIMAL(18,2) DEFAULT NULL AFTER total_disc,
  ADD COLUMN grand_total DECIMAL(18,2) DEFAULT NULL AFTER total_tax,
  ADD COLUMN shipping_cost DECIMAL(18,2) DEFAULT NULL AFTER grand_total,
  ADD COLUMN insurance_cost DECIMAL(18,2) DEFAULT NULL AFTER shipping_cost,
  ADD COLUMN shipping_tax DECIMAL(18,2) DEFAULT NULL AFTER insurance_cost,
  ADD COLUMN shipping_cost_discount DECIMAL(18,2) DEFAULT NULL AFTER shipping_tax,
  ADD COLUMN discount_marketplace DECIMAL(18,2) DEFAULT NULL AFTER shipping_cost_discount,
  ADD COLUMN service_fee DECIMAL(18,2) DEFAULT NULL AFTER discount_marketplace,
  ADD COLUMN order_processing_fee DECIMAL(18,2) DEFAULT NULL AFTER service_fee,
  ADD COLUMN cod_fee DECIMAL(18,2) DEFAULT NULL AFTER order_processing_fee,
  ADD COLUMN buyer_shipping_cost DECIMAL(18,2) DEFAULT NULL AFTER cod_fee,
  ADD COLUMN shipping_full_name VARCHAR(255) DEFAULT NULL AFTER buyer_shipping_cost,
  ADD COLUMN shipping_phone VARCHAR(50) DEFAULT NULL AFTER shipping_full_name,
  ADD COLUMN shipping_address VARCHAR(255) DEFAULT NULL AFTER shipping_phone,
  ADD COLUMN shipping_area VARCHAR(150) DEFAULT NULL AFTER shipping_address,
  ADD COLUMN shipping_city VARCHAR(150) DEFAULT NULL AFTER shipping_area,
  ADD COLUMN shipping_province VARCHAR(150) DEFAULT NULL AFTER shipping_city,
  ADD COLUMN shipping_post_code VARCHAR(50) DEFAULT NULL AFTER shipping_province,
  ADD COLUMN shipping_country VARCHAR(100) DEFAULT NULL AFTER shipping_post_code,
  ADD COLUMN courier VARCHAR(150) DEFAULT NULL AFTER shipping_country,
  ADD COLUMN shipper VARCHAR(150) DEFAULT NULL AFTER courier,
  ADD COLUMN tracking_no VARCHAR(150) DEFAULT NULL AFTER shipper,
  ADD COLUMN tracking_number VARCHAR(150) DEFAULT NULL AFTER tracking_no,
  ADD COLUMN tracking_url VARCHAR(255) DEFAULT NULL AFTER tracking_number;
